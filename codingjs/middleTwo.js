function middleTwo(str){
  // This quiestion is totally wrong
  if (str.length < 3) return '';
  if (str.length % 2 != 0) return str;
//   const startPos = str.length / 2 - 1;
//   return str.substr(startPos, 2);
  return "";
}